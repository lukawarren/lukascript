fn factorial: int number

    if number is 1
        return 1
    done

    factorial(number - 1) -> ret
    return number * ret
done

fn print_factorial: int number

    factorial(5) -> result
    print(number "! = " result)

done

print_factorial(5)